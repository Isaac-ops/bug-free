import math
def fn_1(number):
    row=0
    while (row<number):
        row_spacer=(number-row)*" "
        row_content=(2*row+1)*"$"
        print(row_spacer+row_content)
        row=row+1
    return None

my_input=(int(input("Enter the number of rows:")))
fn_1(my_input)
